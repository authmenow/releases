http:
  middlewares:
    api-internal-stripprefix:
      stripPrefix:
        prefixes:
          - "/api/internal"

tls:
  certificates:
    - certFile: "/etc/tls/accesso-local.cert"
      keyFile: "/etc/tls/accesso-local.pem"